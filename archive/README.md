# Retired analysis

`retired/` contains superseded scripts and earlier reports with original relative paths. Active commands do not use these files.

`previous_analysis.zip` is a verified 14-file snapshot made before cleanup, including the previous reconstruction notes. The duplicate snapshot is retained for recovery.

Earlier consistency reports concern a different workbook version. Archived standalone SHAP output used earlier saved tuning parameters. Neither patient workbooks nor thesis documents were removed.

Automatic approval review blocked deletion, so files were moved here instead. Python caches remain in place and are ignored by Git.
